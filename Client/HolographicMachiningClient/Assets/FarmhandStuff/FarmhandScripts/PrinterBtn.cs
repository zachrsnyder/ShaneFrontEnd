using System.Collections;
using System.Collections.Generic;
using FarmhandStuff;
using Microsoft.MixedReality.Toolkit.UX;
using TMPro;
using Unity.VisualScripting;
using UnityEditor;
using UnityEngine;

public class PrinterBtn : PressableButton
{
    /*
    [SerializeField] public TextMeshProUGUI textLabel;

    private PrinterSelectionController MenuController;

    private Printer _AssociatedPrinter;
    
    public Printer AssociatedPrinter
    {
        get
        {
            return _AssociatedPrinter;
        }

        set
        {
            this.textLabel.text = value.name;
            _AssociatedPrinter = value;
        }
    }

    void Start()
    {
        this.OnClicked.AddListener(onSelect);
        MenuController = GetComponentInParent<PrinterSelectionController>();
        if (!MenuController) Debug.LogError("No Menu Controller found in Hierarchy! This will break things!");
    }

    
    void onSelect()
    {
        MenuController.OnPrinterSelected(this);
    }
    */
}
